package EncapsulationAbstraction.Interfaces.ExtendingInterfaces;

public interface B extends A {
    // interface to interface inheritance we use 'extends' keyword. But class to interface inheritance we use
    // 'implements' keyword
    void greet();
}
